apiVersion: v1
kind: Service
metadata:
  labels:
    app: edgedevice-sample-deployment
  name: edgedevice-sample
spec:
  ports:
  - port: 1234
    protocol: TCP
    targetPort: 8080
  selector:
    app: edgedevice-sample-deployment
  type: LoadBalancer
