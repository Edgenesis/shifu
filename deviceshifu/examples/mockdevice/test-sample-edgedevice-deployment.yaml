apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: edgedevice-sample-deployment
  name: edgedevice-sample-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: edgedevice-sample-deployment
  template:
    metadata:
      labels:
        app: edgedevice-sample-deployment
    spec:
      containers:
      - image: edgehub/deviceshifu-http:v0.0.1
        name: deviceshifu-http
        ports:
        - containerPort: 8080
        volumeMounts:
        - name: edgedevice-config
          mountPath: "/etc/edgedevice/config"
          readOnly: true
      volumes:
      - name: edgedevice-config
        configMap:
          name: mockdevice-configmap-0.0.1
      serviceAccountName: edgedevice-reader-sa
