apiVersion: v1
kind: Service
metadata:
  labels:
    app: edgedevice-robot-deployment
  name: edgedevice-robot
spec:
  ports:
  - port: 1234
    protocol: TCP
    targetPort: 8080
  selector:
    app: edgedevice-robot-deployment
  type: LoadBalancer
