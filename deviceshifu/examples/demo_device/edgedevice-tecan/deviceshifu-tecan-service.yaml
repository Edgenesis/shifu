apiVersion: v1
kind: Service
metadata:
  labels:
    app: edgedevice-tecan-deployment
  name: edgedevice-tecan
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: edgedevice-tecan-deployment
  type: LoadBalancer
