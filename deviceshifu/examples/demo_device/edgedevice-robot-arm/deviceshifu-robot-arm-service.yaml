apiVersion: v1
kind: Service
metadata:
  labels:
    app: deviceshifu-robotarm-deployment
  name: deviceshifu-robotarm
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: deviceshifu-robotarm-deployment
  type: LoadBalancer
