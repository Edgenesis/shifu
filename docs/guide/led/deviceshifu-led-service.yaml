apiVersion: v1
kind: Service
metadata:
  labels:
    app: edgedevice-led-deployment
  name: edgedevice-led
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: edgedevice-led-deployment
  type: LoadBalancer
