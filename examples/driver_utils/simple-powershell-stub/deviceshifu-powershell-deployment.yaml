apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: edgedevice-powershell-deployment
  name: edgedevice-powershell-deployment
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: edgedevice-powershell-deployment
  template:
    metadata:
      labels:
        app: edgedevice-powershell-deployment
    spec:
      containers:
      - image: edgehub/deviceshifu-http-powershell:v0.0.1
        name: deviceshifu-http
        ports:
        - containerPort: 8080
        volumeMounts:
        - name: edgedevice-config
          mountPath: "/etc/edgedevice/config"
          readOnly: true
        env:
        - name: EDGEDEVICE_NAME
          value: "edgedevice-powershell"
        - name: EDGEDEVICE_NAMESPACE
          value: "devices"
      volumes:
      - name: edgedevice-config
        configMap:
          name: powershell-configmap-0.0.1
      serviceAccountName: edgedevice-sa
