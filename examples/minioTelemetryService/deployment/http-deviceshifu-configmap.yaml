apiVersion: v1
kind: ConfigMap
metadata:
  name: http-configmap-0.0.1
  namespace: deviceshifu
data:
  driverProperties: |
    driverSku: HTTP Device
    driverImage: http-device:v0.0.1
  instructions: |
    instructions:
      status:
  telemetries: |
    telemetrySettings:
      telemetryUpdateIntervalInMilliseconds: 6000
      telemetryTimeoutInMilliseconds: 3000
    telemetries:
      device_health1:
        properties:
          instruction: get_file
          pushSettings:
            telemetryCollectionService: push-file-txt
