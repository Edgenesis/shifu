apiVersion: v1
kind: Service
metadata:
  labels:
    app: edgedevice-opcua-deployment
  name: edgedevice-opcua
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: edgedevice-opcua-deployment
  type: LoadBalancer