apiVersion: v1
kind: Service
metadata:
  labels:
    app: edgedevice-camera-deployment
  name: edgedevice-camera
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: edgedevice-camera-deployment
  type: LoadBalancer
