apiVersion: v1
kind: Service
metadata:
  labels:
    app: deviceshifu-camera-deployment
  name: deviceshifu-camera
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: deviceshifu-camera-deployment
  type: LoadBalancer
