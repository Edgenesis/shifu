apiVersion: shifu.edgenesis.io/v1alpha1
kind: EdgeDevice
metadata:
  name: edgedevice-mqtt
  namespace: devices
spec:
  sku: "testMQTT"
  connection: Ethernet
  address: mosquitto-service.devices.svc.cluster.local:18830 # change this accordingly
  protocol: MQTT
  description: |
    MQTT test device. Publishes and subscribes to multiple test topics.
  connectionInfo: |
    MQTT protocol. No authentication required. Use QoS 1 for commands, QoS 0 for status.
  protocolSettings:
    MQTTSetting:
