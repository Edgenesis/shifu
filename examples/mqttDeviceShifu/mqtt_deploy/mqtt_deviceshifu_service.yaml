apiVersion: v1
kind: Service
metadata:
  labels:
    app: deviceshifu-mqtt-deployment
  name: deviceshifu-mqtt
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: deviceshifu-mqtt-deployment
  type: LoadBalancer
